[tool.poetry]
name = "td-data"
version = "0.1.0"
description = ""
authors = ["Chen Xu"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
numpy = "^2.1.0"
pandas = "^2.2.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
